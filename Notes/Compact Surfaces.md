---
tags:
  - Topology
---
Subjects: [[Topology]]
Links: [[Quotient Topology]], [[Topological Manifolds]], [[Cell Complexes and CW Complexes]], [[Simplicial Complexes]]

**Def:** A *surface* is a $2$-manifold.

**Prop:** the sphere $\Bbb S^2$ is homeomorphic to the following quotient spaces.
- The closed disk $\bar{\Bbb B}^2\subseteq \Bbb R^2$ module the equivalence relation generated by $(x, y) \sim (-x, y)$ for $(x, y)\in \partial \bar{\Bbb B}^2$.
- The square region $S = \{(x, y) \mid |x| + |y| \le 1\}$ module the equivalence relation generated by $(x,y) \sim(-x, y)$ for $(x, y)\in \partial S$.

**Prop:** The real projective plane $\Bbb {RP}^2$ is homeomorphic to each of the following quotient spaces.
- The closed disk $\bar{\Bbb B}^2\subseteq \Bbb R^2$ module the equivalence relation generated by $(x, y) \sim (-x, -y)$ for $(x, y)\in \partial \bar{\Bbb B}^2$.
- The square region $S = \{(x, y) \mid |x| + |y| \le 1\}$ module the equivalence relation generated by $(x,y) \sim(-x, -y)$ for $(x, y)\in \partial S$.

**Def:** A *polygon* is a subset of $\Bbb R^2$ that is homeomorphic to $\Bbb S^1$ and is the union of finitely many $1$-simplices that meet only ar their endpoints; thus it is the polyhedron of $1$-dimensional simplicial complex, and a regular finite CW complex. The $0$-simplices and $1$-simplices of the polygon are called its *vertices* and *edges*, respectively. 

A *polygonal region* is a compact subset of $\Bbb R^2$ whose interior is a regular coordinate ball, and whose boundary is a polygon. 

**Prop:** Let $P_1, \dots, P_k$ be polygonal regions in the plane, let $P := P_1 \sqcup \dots \sqcup P_k$, and suppose we are given an equivalence relation on $P$ that identifies some of the edges of the polygon with others by means of affine homeomorphisms.
* The resulting quotient space is a finite $2$-dimensional CW complex whose $0$-skeleton is the image under the quotient map fo the set of vertices of $P$, and whose $1$-skeleton is the image of the union of the boundaries of the polygonal region.
  If the equivalence relation identifies each edge of $P_i$ with exactly one other edge in some $P_j$, then the resulting quotient space is a compact $2$-manifold. 

# Polygonal Representations

Given a set $S$, we define a *word in $S$* to be an ordered $k$-tuple of symbols, each of the form $a$ or $a^{-1}$ for some $a\in S$. A *polygonal representation*, written $$\mathcal P := \langle S \mid W_1, \dots, W_k\rangle, $$ is a finite set $S$ together with finitely many words $W_1, \dots, W_k$ in $S$ of length $3$ or more, such that every symbol in $S$ appears in at least one word. As a matter of notation, when the set $S$ is described by listing the elements, we leave out the braces surrounding the elements of $S$, and we denote the words $W_i$ by juxtaposition. 

Any polygonal representation $\cal P$ determines a topological space $|{\cal P}|$, called the *geometric representation of $\cal P$*, by the following procedure:
- For each word $W_i$, let $P_i$ denote de convex $k$-sided polygonal region in the plane that has its centre at the origin, sides of length $1$, equal angles, and one vertex on the positive $y$-axis. 
- Define a one-to-one correspondence between the symbols $W_i$ and the edges of $P_i$ in counterclockwise orders, starting at the vertex on the $y$-axis.
- Let $|\mathcal P|$ denote the quotient space of $\coprod_i P_i$ determined by identifying edges that have the same edge symbol, according to the affine homeomorphism that matches up the first vertex of those edges with a given label $a$ and the las vertices of those with the corresponding label $a^{-1}$, in counterclockwise order. 

The interiors, edges, and vertices of the polygonal regions $P_i$ are called the *faces, edges, and vertices of the representation*. The number of faces is the number of words, the number of edges is the sum of the length of the words. For an edge labelled $a$, the *initial vertex* is the first one in the counterclockwise order, and the *terminal vertex* is the other one; for an edge labelled $a^{-1}$, these definitions are reversed. 

A polygonal representation is called a *surface presentation* if each symbol $a\in S$ occurs exactly twice in $W_1, \dots, W_k$ (we are counting $a$ or $a^{-1}$ as one occurrence). By the proposition preceding this section we know that the geometric realisation of surface presentation is a compact surface. 

If $X$ is a topological space and $\cal P$ is a polygonal presentation whose geometric realisation is homeomorphic to $X$, we say that $\cal P$ is a *presentation of $X$*. 

If two presentation ${\cal P}_1$ and ${\cal P}_2$ have homeomorphic realisations, we say that they are *topologically equivalent* and write ${\cal P}_1 \approx {\cal P}_2$. 

Let $S$ denote any sequence of symbols, $e$ denotes any symbol not in $S$. The following operations are called *elementary transformations* of a polygonal presentation.
- Relabelling: Changing all occurrences of a symbol $a$ to a new symbol not already in the presentation, interchanging all occurrences of two symbols $a$ and $b$, or interchanging all occurrences of $a$ and $a^{-1}$ for some $a\in S$. 
- Subdividing: Replacing every occurrence of $a$ by $ae$ and every occurrence of $a^{-1}$ by $e^{-1}a^{-1}$, where $e$ is a new symbol not already in the presentation.
- Consolidating: If $a$ and $b$ always occur adjacent to each other either as $ab$ or $b^{-1}a^{-1}$, replacing every occurrence of $ab$ by $a$ and every occurrence of $b^{-1}a^{-1}$ by $a^{-1}$, provided that the result is one or more words of length at least $3$ or a simple word of length $2$. 
- Reflecting: $$\langle S \mid a_1\cdots a_m, W_2, \dots, W_k\rangle \mapsto \langle S \mid a_m^{-1}\cdots a_1^{-1}, W_2, \dots, W_k\rangle$$
- Rotating: $$\langle S \mid a_1a_2\cdots a_m, W_2, \dots, W_k\rangle \mapsto \langle S \mid a_2\cdots a_ma_1, W_2, \dots, W_k\rangle$$
- Cutting: If $W_1$ and $W_2$ both have length of at least $2$, $$\langle S \mid W_1 W_2, \dots, W_k\rangle \mapsto \langle S, e \mid W_1e, e^{-1}W_2, \dots, W_k\rangle$$
- Pasting: $$\langle S, e \mid W_1e, e^{-1}W_2, \dots, W_k\rangle \mapsto \langle S \mid W_1 W_2, \dots, W_k\rangle $$
- Folding: If $W_1$ has length of at least $3$, $$\langle S, e \mid W_1e e^{-1}, W_2, \dots, W_k\rangle \mapsto \langle S \mid W_1, W_2, \dots, W_k\rangle $$
- Unfolding: $$\langle S \mid W_1, W_2, \dots, W_k\rangle\mapsto \langle S, e \mid W_1e e^{-1}, W_2, \dots, W_k\rangle $$

**Prop:** Each elementary transformation of a polygonal presentation produces a topologically equivalent presentation.

**Prop:** Let $M_1$ and $M_2$ be surfaces that admit presentations $\langle S_1\mid W_1\rangle$ and $\langle S_2\mid W_2\rangle$, respectively, in which $S_1$ and $S_2$ are disjoint sets and each presentation has a single face. Then $\langle S_1, S_2\mid W_1W_2\rangle$ is a presentation of connected sum $M_1 \# M_2$. Here $W_1 W_2$ denotes the word formed by concatenating $W_1$ and $W_2$.

**Cor:** Using the preceding proposition, we can list presentations of known surfaces. 
- Sphere: $\langle a \mid aa^{-1}\rangle$.
- Connected sum of $n\ge 1$ tori: $$\langle a_1, b_1,\dots, a_n, b_n \mid a_1b_1a_1^{-1}b_1^{-1}\cdots a_n b_n a_n^{-1}b^{-1}_n\rangle. $$
- Connected sum of $n \ge 1$ projective real planes: $$\langle a_1,\dots, a_n, \mid a_1a_1\cdots a_n a_n\rangle. $$
We call the *standard presentations* of these surfaces.

**Prop:** Every compact surface admits a polygonal presentation. To prove this fact we need to use that every $2$-manifold is [[Simplicial Complexes#^e0f03b|triangulable]]. 

